#!/usr/bin/sh

editor="$EDITOR"
if [ -z "$editor" ]; then
	editor='nvim'
fi

files="$( find ~ -name '*.txt' -o -name '*.md' -o -name '*.tex' -o -name '*.gmi' \
	| grep --invert-match '/\.' \
	| fzf --multi )"

if [ -n "$files" ]; then
	printf "editing:\n$files\n" >&2
	exec $editor $files
fi
